files:
  "/etc/nginx/conf.d/elasticbeanstalk/00_application.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      upstream nodejs {
        server 127.0.0.1:5000; # 여러분의 서버가 5000번 포트를 사용하므로 5000으로 설정!
        keepalive 256;
      }